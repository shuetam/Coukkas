using System;

using System.Threading.Tasks;
using System.Timers;
using Coukkas.Core;
using Coukkas.Core.Domain;
using Coukkas.Infrastructure;
using FluentAssertions;
using Xunit;

namespace Tests
{
    public class CouponTests
    {
       /*  [Fact]
        public async Task coupon_should_change_location_after_1_minute()
        {
            // Arrange
             var fence = new Fence(Guid.NewGuid(), Guid.NewGuid(),"fence1","description1", DateTime.UtcNow,DateTime.UtcNow.AddDays(2),3,3,3);
             var coupon = new Coupon(Guid.NewGuid(), fence.Id, 50, DateTime.UtcNow.AddDays(2), fence);
             var location1 = coupon.location.Latitude;

            Task <double?> location_after_one_minute ()
            {
                System.Threading.Thread.Sleep((int)TimeSpan.FromMinutes(1.1).TotalMilliseconds);
                return Task.FromResult(coupon.location.Latitude);
            }
            //Act
            var location2 = await location_after_one_minute();

            //Assert
          // Assert.NotEqual(location1, location2);
          location2.Should().NotBe(location1);
        } */
        }
    }

